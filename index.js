//Requirements
const express = require('express');
const app = express();
const mongoose = require('mongoose');
const dotenv = require('dotenv').config();
//cors = cross origin resources sharing
//to connect frontend and backend we need cors
const cors = require('cors');
